# MinerU and its dependencies — installed separately to avoid version conflicts
# Install after core requirements: pip install --no-index -f wheelhouse -r requirements-mineru.txt
mineru[core]==2.7.6
magic-pdf>=1.3.0,<2.0.0
