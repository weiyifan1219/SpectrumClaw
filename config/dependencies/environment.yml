# Conda environment definition; companion pip requirements live beside it.
name: SpectrumClaw
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements.txt
