llm:
  mode: api
  provider: openai_compatible
  base_url_env: SPECTRUMCLAW_LLM_BASE_URL
  api_key_env: SPECTRUMCLAW_LLM_API_KEY
  model_env: SPECTRUMCLAW_LLM_MODEL
  local_model:
    enabled: false
    note: 当前没有本地部署大模型需求，仅保留未来接口位置。
