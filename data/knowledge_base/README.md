# Knowledge Base

当前第一批资料库是项目根目录的：

```text
itu_documents.zip
```

当前不移动、不解压。后续实现 RAG 时再将其解压到 `raw/itu/` 并构建索引。

| 子目录 | 用途 |
| --- | --- |
| `raw/` | 解压后的原始文档 |
| `index/` | 文本 chunk、向量或稀疏索引 |
| `graph/` | 后续知识图谱文件 |
